# -*- coding: utf-8 -*- #
# Copyright 2026 Google LLC. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This file is autogenerated and should not be edited by hand.
- release_tracks:
    - GA
  auto_generated: true
  help_text:
    brief: Update the parameters of a single connection
    description: |
      Update a connection.
    examples: |-
      To update the labels of a connection `my-connection` in location `us-central1` run:

      $ {command} my-connection --labels=key1=value1 --location=us-central1
  arguments:
    params:
      - help_text: |-
          The resource name of the connection, in the format
          `projects/{project}/locations/{location}/connections/{connection_id}`.
        is_positional: true
        is_primary_resource: true
        resource_spec:
          attributes:
            - attribute_name: project
              help: The project id of the {resource} resource.
              parameter_name: projectsId
              property: core/project
            - attribute_name: location
              help: The location id of the {resource} resource.
              parameter_name: locationsId
            - attribute_name: connection
              help: The connection id of the {resource} resource.
              parameter_name: connectionsId
          collection: developerconnect.projects.locations.connections
          disable_auto_completers: false
          name: connection
          plural_name: connections
        required: true
      - arg_name: github-config
        api_field: connection.githubConfig
        help_text: Configuration for connections to github.com.
        is_positional: false
        required: false
        type: arg_object
      - arg_name: github-enterprise-config
        api_field: connection.githubEnterpriseConfig
        help_text: Configuration for connections to an instance of GitHub Enterprise.
        is_positional: false
        required: false
        type: arg_object
      - arg_name: gitlab-config
        api_field: connection.gitlabConfig
        help_text: Configuration for connections to gitlab.com.
        is_positional: false
        required: false
        type: arg_object
      - arg_name: gitlab-enterprise-config
        api_field: connection.gitlabEnterpriseConfig
        help_text: Configuration for connections to an instance of GitLab Enterprise.
        is_positional: false
        required: false
        type: arg_object
      - arg_name: bitbucket-data-center-config
        api_field: connection.bitbucketDataCenterConfig
        help_text: Configuration for connections to an instance of Bitbucket Data Center.
        is_positional: false
        required: false
        type: arg_object
      - arg_name: bitbucket-cloud-config
        api_field: connection.bitbucketCloudConfig
        help_text: Configuration for connections to an instance of Bitbucket Clouds.
        is_positional: false
        required: false
        type: arg_object
      - arg_name: secure-source-manager-instance-config
        api_field: connection.secureSourceManagerInstanceConfig
        help_text: Configuration for connections to an instance of Secure Source Manager.
        is_positional: false
        required: false
        type: arg_object
      - arg_name: http-config
        api_field: connection.httpConfig
        help_text: Configuration for connections to an HTTP service provider.
        is_positional: false
        required: false
        type: arg_object
      - arg_name: labels
        api_field: connection.labels
        help_text: Labels as key value pairs
        is_positional: false
        required: false
        repeated: true
        clearable: true
        spec:
          - api_field: key
          - api_field: value
      - arg_name: disabled
        api_field: connection.disabled
        help_text: |-
          If disabled is set to true, functionality is disabled for this
          connection. Repository based API methods and webhooks processing for
          repositories in this connection will be disabled.
        action: store_true_false
        is_positional: false
        required: false
        type: bool
      - arg_name: annotations
        api_field: connection.annotations
        help_text: Allows clients to store small amounts of arbitrary data.
        is_positional: false
        required: false
        repeated: true
        clearable: true
        spec:
          - api_field: key
          - api_field: value
      - arg_name: etag
        api_field: connection.etag
        help_text: |-
          This checksum is computed by the server based on the value of
          other fields, and may be sent on update and delete requests to ensure the
          client has an up-to-date value before proceeding.
        is_positional: false
        required: false
      - arg_name: crypto-key-config
        api_field: connection.cryptoKeyConfig
        help_text: |-
          The crypto key configuration. This field is used by the
          Customer-Managed Encryption Keys (CMEK) feature.
        is_positional: false
        required: false
        type: arg_object
      - arg_name: git-proxy-config
        api_field: connection.gitProxyConfig
        help_text: |-
          Configuration for the git proxy feature. Enabling the git proxy
          allows clients to perform git operations on the repositories linked in the
          connection.
        is_positional: false
        required: false
        type: arg_object
      - arg_name: request-id
        api_field: requestId
        help_text: |-
          An optional request ID to identify requests. Specify a unique
          request ID so that if you must retry your request, the server will know to
          ignore the request if it has already been completed. The server will
          guarantee that for at least 60 minutes since the first request.

          For example, consider a situation where you make an initial request and the
          request times out. If you make the request again with the same request
          ID, the server can check if original operation with the same request ID
          was received, and if so, will ignore the second request. This prevents
          clients from accidentally creating duplicate commitments.

          The request ID must be a valid UUID with the exception that zero UUID is
          not supported (00000000-0000-0000-0000-000000000000).
        is_positional: false
        required: false
      - arg_name: allow-missing
        api_field: allowMissing
        help_text: |-
          If set to true, and the connection is not found a new connection
          will be created. In this situation `update_mask` is ignored.
          The creation will succeed only if the input connection has all the
          necessary information (e.g a github_config with both  user_oauth_token and
          installation_id properties).
        action: store_true_false
        is_positional: false
        required: false
        type: bool
      - arg_name: validate-only
        api_field: validateOnly
        help_text: If set, validate the request, but do not actually post it.
        action: store_true_false
        is_positional: false
        required: false
        type: bool
  request:
    api_version: v1
    collection:
      - developerconnect.projects.locations.connections
    static_fields:
      updateMask: '*'
  async:
    collection:
      - developerconnect.projects.locations.operations
    extract_resource_result: true
  update:
    disable_auto_field_mask: true
    read_modify_update: true
